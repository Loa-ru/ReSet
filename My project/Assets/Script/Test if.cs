using UnityEngine;

public class Testif : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int points = 17;
        int test = 3;
        float qwe = 1.0f * points/test;
        Debug.Log(qwe);
    }
}
